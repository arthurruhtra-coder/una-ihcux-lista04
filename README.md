# una-ihcux-lista04
O comando try-catch é uma estrutura usada para tratar erros ou exceções durante a execução do código, evitando assim que ele quebre de forma inesperada.
  Sendo ''try'' você coloca o código que pode vir a gerar um erro.
  Sendo ''catch'' você define o que fazer se o erro for gerado.
